import Rule from './_base.js';

export const ERROR_MESSAGE = 'TODO';

export default class <%- titleCase %> extends Rule {
  /**
  * @returns {import('./types.js').VisitorReturnType<<%- titleCase %>>}
  */
  visitor() {
    return {
      // Simple template example: disallowed text present in TextNode
      TextNode(node) {
        let source = this.sourceForNode(node);
        let disallowedText = 'DisallowedText';
        let failingCondition = source.includes(disallowedText);

        if (failingCondition) {
          this.log({
            message: ERROR_MESSAGE,
            node,
          });
        }
      },
    };
  }
}
